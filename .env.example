# Get a key at https://platform.claude.com/settings/keys
# Copy this file to .env and fill it in. .env is gitignored — never commit your key.
ANTHROPIC_API_KEY=sk-ant-...

# Port for the analysis API (Vite proxies /api here in dev).
PORT=8787
